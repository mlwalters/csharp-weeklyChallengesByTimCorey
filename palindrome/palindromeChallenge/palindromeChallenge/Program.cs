using System;

namespace palindromeChallenge
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            // to char array
            // remove extra characters & spaces, probably use regex
            // then slice one string at a time 
            // and move first letter to last
            // then compare original and final variable

        }
    }
}
